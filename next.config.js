/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Ensure route handlers work properly
  },
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '*.supabase.co',
      },
      {
        protocol: 'https',
        hostname: 'lh3.googleusercontent.com',
      },
    ],
  },
};

module.exports = nextConfig;
